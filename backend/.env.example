# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/fairtalk
# Set to `true` to require camera verification; set to `false` to bypass (dev)
CAMERA_VERIFICATION=true

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your_password_here (uncomment if using password-protected Redis)

# WebSocket
WS_PORT=5000

# API Keys (if needed)
# OPENAI_API_KEY=your_key_here
