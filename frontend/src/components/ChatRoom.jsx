const ChatRoom = () => {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-950 font-sans">
            <div className="text-center">
                <h1 className="text-4xl font-bold text-gray-700 tracking-wider uppercase select-none">
                    Chat Room
                </h1>
                <p className="text-gray-800 mt-2 text-sm">(Empty for now)</p>
            </div>
        </div>
    );
};

export default ChatRoom;
